# HOST
HOSTNAME=localhost
HOSTNAME_EUREKA=localhost

# Http
HTTP=http

# Version
VERSION=api/v1

# PORT
PORT_CONFIG_SERVER=8888
PORT_DISCOVERY=8761
PORT_GATEWAY=8222
PORT_SCHOOL=8070
PORT_STUDENT=8090

# URI
URI_STUDENT=${HTTP}://${HOSTNAME}:${PORT_STUDENT}
URI_SCHOOL=${HTTP}://${HOSTNAME}:${PORT_SCHOOL}
URI_EUREKA_SERVER_URL=${HTTP}://${HOSTNAME_EUREKA}:${PORT_DISCOVERY}/eureka
URI_GATEWAY=${HTTP}://${HOSTNAME}:${PORT_GATEWAY}
URI_CONFIG_SERVER=${HTTP}://${HOSTNAME}:${PORT_CONFIG_SERVER}

# FEIGN
URI_FEIGN_STUDENT=${URI_GATEWAY}/${VERSION}/students

# DB
DATABASE_SCHOOL=schools
DATABASE_STUDENT=students
DATABASE_USERNAME=admin
DATABASE_PASSWORD=admin
DATABASE_PORT=5434
DATABASE_HOST=localhost
DATABASE_SSL=disable
